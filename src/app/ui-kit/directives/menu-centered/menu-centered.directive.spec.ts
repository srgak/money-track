import { MenuCenteredDirective } from './menu-centered.directive';

describe('MenuCenteredDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuCenteredDirective();
    expect(directive).toBeTruthy();
  });
});
